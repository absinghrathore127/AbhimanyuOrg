trigger ExampleTryTrigger on Contact (after delete, after insert, after undelete, after update, before delete, before insert, before update) {
    
    
    /*
    ExampleTryTriggerHelper.newContact = Trigger.New;
    ExampleTryTriggerHelper.oldContact = Trigger.old;
    ExampleTryTriggerHelper.newMapContact = Trigger.NewMap;
    ExampleTryTriggerHelper.oldMapContact = Trigger.OldMap;
    
    if(Trigger.isAfter){
    	if(Trigger.isInsert) {
    		ExampleTryTriggerHelper.insertContact();
    	}
    	if(Trigger.isUpdate) {
    		ExampleTryTriggerHelper.updateContact();
    	}
    	if(Trigger.isDelete) {
    		ExampleTryTriggerHelper.deleteContact();
    	}
   
    }
    */
    
}